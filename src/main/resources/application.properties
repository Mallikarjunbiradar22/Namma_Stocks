spring.application.name=stock-market
server.port=8081

# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/stock_market
spring.datasource.username=postgres
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.jakarta.persistence.validation.mode=none

# Mail Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=maheshbiradar698@gmail.com
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Admin Credentials
admin.email=admin@gmail.com
admin.password=admin

# Stock API
stock.api.key=ZIDEXXI7SEETRK5M

# Razorpay
razor-pay.api.key=${STOCK_API_KEY}
razor-pay.api.secret=${RAZORPAY_SECRET}

# Platform Percentage
platformPercentage=0.04

# Session
server.servlet.session.tracking-modes=cookie
